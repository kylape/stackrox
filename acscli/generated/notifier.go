// Code generated by acscli/tools/generate. DO NOT EDIT.

package generated

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"

	"github.com/spf13/cobra"
	"github.com/stackrox/rox/acscli/internal/client"
	"github.com/stackrox/rox/acscli/internal/fieldmask"
)

// NewNotifierCmd creates the notifier service command
func NewNotifierCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "notifier",
		Short: "Notifier service operations",
		Long:  "Operations for the Notifier service.\n\nUse 'acs schema notifier' to see available methods.",
	}

	cmd.AddCommand(newNotifierDeletenotifierCmd())

	cmd.AddCommand(newNotifierGetnotifierCmd())

	cmd.AddCommand(newNotifierGetnotifiersCmd())

	cmd.AddCommand(newNotifierPostnotifierCmd())

	cmd.AddCommand(newNotifierPosttestnotifierCmd())

	cmd.AddCommand(newNotifierPosttestupdatednotifierCmd())

	cmd.AddCommand(newNotifierPutnotifierCmd())

	cmd.AddCommand(newNotifierUpdatenotifierCmd())

	return cmd
}

func newNotifierDeletenotifierCmd() *cobra.Command {
	var (
		flagId string

		flagForce bool

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "deletenotifier",
		Short: "DeleteNotifier removes a notifier configuration given its ID.",
		Long:  "DeleteNotifier removes a notifier configuration given its ID.\n\nHTTP: DELETE /v1/notifiers/{id}",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/{id}"

			path = strings.ReplaceAll(path, "{id}", fmt.Sprintf("%v", flagId))

			// Build query parameters
			var queryParams []string

			if flagForce {
				queryParams = append(queryParams, fmt.Sprintf("force=%v", flagForce))
			}

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "DELETE",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Delete(path)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagId, "id", "", "")

	cmd.MarkFlagRequired("id")

	cmd.Flags().BoolVar(&flagForce, "force", false, "")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierGetnotifierCmd() *cobra.Command {
	var (
		flagId string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "getnotifier",
		Short: "GetNotifier returns the notifier configuration given its ID.",
		Long:  "GetNotifier returns the notifier configuration given its ID.\n\nHTTP: GET /v1/notifiers/{id}",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/{id}"

			path = strings.ReplaceAll(path, "{id}", fmt.Sprintf("%v", flagId))

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "GET",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Get(path)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagId, "id", "", "")

	cmd.MarkFlagRequired("id")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierGetnotifiersCmd() *cobra.Command {
	var (
		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "getnotifiers",
		Short: "GetNotifiers returns all notifier configurations.",
		Long:  "GetNotifiers returns all notifier configurations.\n\nHTTP: GET /v1/notifiers",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers"

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "GET",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Get(path)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierPostnotifierCmd() *cobra.Command {
	var (
		jsonInput string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "postnotifier",
		Short: "PostNotifier creates a notifier configuration.",
		Long:  "PostNotifier creates a notifier configuration.\n\nHTTP: POST /v1/notifiers",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers"

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "POST",
					"path":    path,

					"has_body":    true,
					"body_source": jsonInput,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			var body interface{}
			if jsonInput != "" {
				if strings.HasPrefix(jsonInput, "@") {
					data, err := os.ReadFile(strings.TrimPrefix(jsonInput, "@"))
					if err != nil {
						return fmt.Errorf("failed to read JSON file: %w", err)
					}
					if err := json.Unmarshal(data, &body); err != nil {
						return fmt.Errorf("invalid JSON in file: %w", err)
					}
				} else {
					if err := json.Unmarshal([]byte(jsonInput), &body); err != nil {
						return fmt.Errorf("invalid JSON: %w", err)
					}
				}
			}

			resp, err = c.Post(path, body)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&jsonInput, "json", "", "JSON request body (use @filename to read from file)")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierPosttestnotifierCmd() *cobra.Command {
	var (
		jsonInput string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "posttestnotifier",
		Short: "TestNotifier checks if a notifier is correctly configured.",
		Long:  "TestNotifier checks if a notifier is correctly configured.\n\nHTTP: POST /v1/notifiers/test",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/test"

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "POST",
					"path":    path,

					"has_body":    true,
					"body_source": jsonInput,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			var body interface{}
			if jsonInput != "" {
				if strings.HasPrefix(jsonInput, "@") {
					data, err := os.ReadFile(strings.TrimPrefix(jsonInput, "@"))
					if err != nil {
						return fmt.Errorf("failed to read JSON file: %w", err)
					}
					if err := json.Unmarshal(data, &body); err != nil {
						return fmt.Errorf("invalid JSON in file: %w", err)
					}
				} else {
					if err := json.Unmarshal([]byte(jsonInput), &body); err != nil {
						return fmt.Errorf("invalid JSON: %w", err)
					}
				}
			}

			resp, err = c.Post(path, body)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&jsonInput, "json", "", "JSON request body (use @filename to read from file)")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierPosttestupdatednotifierCmd() *cobra.Command {
	var (
		jsonInput string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "posttestupdatednotifier",
		Short: "TestUpdatedNotifier checks if the given notifier is correctly configured, with optional stored credential reconciliation.",
		Long:  "TestUpdatedNotifier checks if the given notifier is correctly configured, with optional stored credential reconciliation.\n\nHTTP: POST /v1/notifiers/test/updated",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/test/updated"

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "POST",
					"path":    path,

					"has_body":    true,
					"body_source": jsonInput,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			var body interface{}
			if jsonInput != "" {
				if strings.HasPrefix(jsonInput, "@") {
					data, err := os.ReadFile(strings.TrimPrefix(jsonInput, "@"))
					if err != nil {
						return fmt.Errorf("failed to read JSON file: %w", err)
					}
					if err := json.Unmarshal(data, &body); err != nil {
						return fmt.Errorf("invalid JSON in file: %w", err)
					}
				} else {
					if err := json.Unmarshal([]byte(jsonInput), &body); err != nil {
						return fmt.Errorf("invalid JSON: %w", err)
					}
				}
			}

			resp, err = c.Post(path, body)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&jsonInput, "json", "", "JSON request body (use @filename to read from file)")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierPutnotifierCmd() *cobra.Command {
	var (
		flagId string

		jsonInput string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "putnotifier",
		Short: "PutNotifier modifies a given notifier, without using stored credential reconciliation.",
		Long:  "PutNotifier modifies a given notifier, without using stored credential reconciliation.\n\nHTTP: PUT /v1/notifiers/{id}",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/{id}"

			path = strings.ReplaceAll(path, "{id}", fmt.Sprintf("%v", flagId))

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "PUT",
					"path":    path,

					"has_body":    true,
					"body_source": jsonInput,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			var body interface{}
			if jsonInput != "" {
				if strings.HasPrefix(jsonInput, "@") {
					data, err := os.ReadFile(strings.TrimPrefix(jsonInput, "@"))
					if err != nil {
						return fmt.Errorf("failed to read JSON file: %w", err)
					}
					if err := json.Unmarshal(data, &body); err != nil {
						return fmt.Errorf("invalid JSON in file: %w", err)
					}
				} else {
					if err := json.Unmarshal([]byte(jsonInput), &body); err != nil {
						return fmt.Errorf("invalid JSON: %w", err)
					}
				}
			}

			resp, err = c.Put(path, body)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagId, "id", "", "")

	cmd.MarkFlagRequired("id")

	cmd.Flags().StringVar(&jsonInput, "json", "", "JSON request body (use @filename to read from file)")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}

func newNotifierUpdatenotifierCmd() *cobra.Command {
	var (
		flagNotifierId string

		jsonInput string

		dryRun    bool
		outputFmt string
		fields    string
	)

	cmd := &cobra.Command{
		Use:   "updatenotifier",
		Short: "UpdateNotifier modifies a given notifier, with optional stored credential reconciliation.",
		Long:  "UpdateNotifier modifies a given notifier, with optional stored credential reconciliation.\n\nHTTP: PATCH /v1/notifiers/{notifier.id}",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/notifiers/{notifier.id}"

			path = strings.ReplaceAll(path, "{notifier.id}", fmt.Sprintf("%v", flagNotifierId))

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "PATCH",
					"path":    path,

					"has_body":    true,
					"body_source": jsonInput,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			var body interface{}
			if jsonInput != "" {
				if strings.HasPrefix(jsonInput, "@") {
					data, err := os.ReadFile(strings.TrimPrefix(jsonInput, "@"))
					if err != nil {
						return fmt.Errorf("failed to read JSON file: %w", err)
					}
					if err := json.Unmarshal(data, &body); err != nil {
						return fmt.Errorf("invalid JSON in file: %w", err)
					}
				} else {
					if err := json.Unmarshal([]byte(jsonInput), &body); err != nil {
						return fmt.Errorf("invalid JSON: %w", err)
					}
				}
			}

			resp, err = c.Request("PATCH", path, body)

			if err != nil {
				return err
			}

			// Apply field mask if specified
			if fields != "" {
				resp, err = fieldmask.Apply(resp, fields)
				if err != nil {
					return fmt.Errorf("failed to apply field mask: %w", err)
				}
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagNotifierId, "notifier-id", "", "")

	cmd.MarkFlagRequired("notifier-id")

	cmd.Flags().StringVar(&jsonInput, "json", "", "JSON request body (use @filename to read from file)")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")
	cmd.Flags().StringVar(&fields, "fields", "", "Comma-separated fields to include (e.g., id,name,policies[].severity)")

	return cmd
}
