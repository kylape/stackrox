// Code generated by acscli/tools/generate. DO NOT EDIT.

package generated

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"

	"github.com/spf13/cobra"
	"github.com/stackrox/rox/acscli/internal/client"
)

// NewAdministrationEventsCmd creates the administration_events service command
func NewAdministrationEventsCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "administration_events",
		Short: "Administration Events service operations",
		Long:  "Operations for the Administration Events service.\n\nUse 'acs schema administration_events' to see available methods.",
	}

	cmd.AddCommand(newAdministrationEventsCountadministrationeventsCmd())

	cmd.AddCommand(newAdministrationEventsGetadministrationeventCmd())

	cmd.AddCommand(newAdministrationEventsListadministrationeventsCmd())

	return cmd
}

func newAdministrationEventsCountadministrationeventsCmd() *cobra.Command {
	var (
		flagFilterFrom string

		flagFilterUntil string

		dryRun    bool
		outputFmt string
	)

	cmd := &cobra.Command{
		Use:   "countadministrationevents",
		Short: "CountAdministrationEvents returns the number of events after filtering by requested fields.",
		Long:  "CountAdministrationEvents returns the number of events after filtering by requested fields.\n\nHTTP: GET /v1/count/administration/events",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/count/administration/events"

			// Build query parameters
			var queryParams []string

			if flagFilterFrom != "" {
				queryParams = append(queryParams, fmt.Sprintf("filter.from=%s", flagFilterFrom))
			}

			if flagFilterUntil != "" {
				queryParams = append(queryParams, fmt.Sprintf("filter.until=%s", flagFilterUntil))
			}

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "GET",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Get(path)

			if err != nil {
				return err
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagFilterFrom, "filter-from", "", "Matches events with last_occurred_at after a specific timestamp, i.e. the lower boundary.")

	cmd.Flags().StringVar(&flagFilterUntil, "filter-until", "", "Matches events with last_occurred_at before a specific timestamp, i.e. the upper boundary.")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")

	return cmd
}

func newAdministrationEventsGetadministrationeventCmd() *cobra.Command {
	var (
		flagId string

		dryRun    bool
		outputFmt string
	)

	cmd := &cobra.Command{
		Use:   "getadministrationevent",
		Short: "GetAdministrationEvent retrieves an event by ID.",
		Long:  "GetAdministrationEvent retrieves an event by ID.\n\nHTTP: GET /v1/administration/events/{id}",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/administration/events/{id}"

			path = strings.ReplaceAll(path, "{id}", fmt.Sprintf("%v", flagId))

			// Build query parameters
			var queryParams []string

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "GET",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Get(path)

			if err != nil {
				return err
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().StringVar(&flagId, "id", "", "")

	cmd.MarkFlagRequired("id")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")

	return cmd
}

func newAdministrationEventsListadministrationeventsCmd() *cobra.Command {
	var (
		flagPaginationLimit int32

		flagPaginationOffset int32

		flagPaginationSortOptionField string

		flagPaginationSortOptionReversed bool

		flagPaginationSortOptionAggregateByAggrFunc string

		flagPaginationSortOptionAggregateByDistinct bool

		flagFilterFrom string

		flagFilterUntil string

		dryRun    bool
		outputFmt string
	)

	cmd := &cobra.Command{
		Use:   "listadministrationevents",
		Short: "ListAdministrationEvents returns the list of events after filtered by requested fields.",
		Long:  "ListAdministrationEvents returns the list of events after filtered by requested fields.\n\nHTTP: GET /v1/administration/events",
		RunE: func(cmd *cobra.Command, args []string) error {
			// Build request
			cfg := client.Config{
				Endpoint: os.Getenv("ROX_ENDPOINT"),
				Token:    os.Getenv("ROX_API_TOKEN"),
				Insecure: os.Getenv("ROX_INSECURE") == "true",
			}

			c, err := client.NewClient(cfg)
			if err != nil {
				return err
			}

			// Build path with path parameters
			path := "/v1/administration/events"

			// Build query parameters
			var queryParams []string

			if flagPaginationLimit != 0 {
				queryParams = append(queryParams, fmt.Sprintf("pagination.limit=%v", flagPaginationLimit))
			}

			if flagPaginationOffset != 0 {
				queryParams = append(queryParams, fmt.Sprintf("pagination.offset=%v", flagPaginationOffset))
			}

			if flagPaginationSortOptionField != "" {
				queryParams = append(queryParams, fmt.Sprintf("pagination.sortOption.field=%s", flagPaginationSortOptionField))
			}

			if flagPaginationSortOptionReversed {
				queryParams = append(queryParams, fmt.Sprintf("pagination.sortOption.reversed=%v", flagPaginationSortOptionReversed))
			}

			if flagPaginationSortOptionAggregateByAggrFunc != "" {
				queryParams = append(queryParams, fmt.Sprintf("pagination.sortOption.aggregateBy.aggrFunc=%s", flagPaginationSortOptionAggregateByAggrFunc))
			}

			if flagPaginationSortOptionAggregateByDistinct {
				queryParams = append(queryParams, fmt.Sprintf("pagination.sortOption.aggregateBy.distinct=%v", flagPaginationSortOptionAggregateByDistinct))
			}

			if flagFilterFrom != "" {
				queryParams = append(queryParams, fmt.Sprintf("filter.from=%s", flagFilterFrom))
			}

			if flagFilterUntil != "" {
				queryParams = append(queryParams, fmt.Sprintf("filter.until=%s", flagFilterUntil))
			}

			if len(queryParams) > 0 {
				path = path + "?" + strings.Join(queryParams, "&")
			}

			// Handle dry-run
			if dryRun {
				result := map[string]interface{}{
					"dry_run": true,
					"method":  "GET",
					"path":    path,
				}
				return outputResult(result, outputFmt)
			}

			// Make request
			var resp []byte

			resp, err = c.Get(path)

			if err != nil {
				return err
			}

			// Parse and output response
			var result interface{}
			if err := json.Unmarshal(resp, &result); err != nil {
				// Return raw response if not JSON
				fmt.Println(string(resp))
				return nil
			}

			return outputResult(result, outputFmt)
		},
	}

	// Add flags

	cmd.Flags().Int32Var(&flagPaginationLimit, "pagination-limit", 0, "")

	cmd.Flags().Int32Var(&flagPaginationOffset, "pagination-offset", 0, "")

	cmd.Flags().StringVar(&flagPaginationSortOptionField, "pagination-sort-option-field", "", "")

	cmd.Flags().BoolVar(&flagPaginationSortOptionReversed, "pagination-sort-option-reversed", false, "")

	cmd.Flags().StringVar(&flagPaginationSortOptionAggregateByAggrFunc, "pagination-sort-option-aggregate-by-aggr-func", "", "")

	cmd.Flags().BoolVar(&flagPaginationSortOptionAggregateByDistinct, "pagination-sort-option-aggregate-by-distinct", false, "")

	cmd.Flags().StringVar(&flagFilterFrom, "filter-from", "", "Matches events with last_occurred_at after a specific timestamp, i.e. the lower boundary.")

	cmd.Flags().StringVar(&flagFilterUntil, "filter-until", "", "Matches events with last_occurred_at before a specific timestamp, i.e. the upper boundary.")

	cmd.Flags().BoolVar(&dryRun, "dry-run", false, "Preview request without executing")
	cmd.Flags().StringVarP(&outputFmt, "output", "o", "", "Output format: json, table (default: auto)")

	return cmd
}
