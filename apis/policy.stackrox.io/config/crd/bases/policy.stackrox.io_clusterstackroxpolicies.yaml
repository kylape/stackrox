---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: clusterstackroxpolicies.policy.stackrox.io
spec:
  group: policy.stackrox.io
  names:
    kind: ClusterStackroxPolicy
    listKind: ClusterStackroxPolicyList
    plural: clusterstackroxpolicies
    shortNames:
    - csrxp
    - csp
    singular: clusterstackroxpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.policyName
      name: Policy Name
      type: string
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=="AcceptedBySensor")].status
      name: Accepted
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterStackroxPolicy is the schema for cluster-scoped policies in secured clusters
          These policies are evaluated locally and never sent to Central's policy API
          Policy definitions are sent to Central only when violations occur (embedded in alerts)
          Cluster-scoped policies can target resources across all namespaces
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterStackroxPolicySpec defines the desired state of ClusterStackroxPolicy
              This is similar to StackroxPolicySpec but for cluster-scoped policies
              Cluster-scoped policies can target resources across all namespaces in the cluster
            properties:
              categories:
                description: Categories is a list of categories that this policy falls
                  under
                items:
                  type: string
                minItems: 1
                type: array
              description:
                description: Description is a free-form text description of this policy
                pattern: ^[^\$]{0,800}$
                type: string
              disabled:
                description: Disabled toggles whether this policy will execute and
                  fire alerts
                type: boolean
              enforcementActions:
                description: EnforcementActions lists the enforcement actions to take
                  when a violation is identified
                items:
                  description: EnforcementAction defines enforcement actions for policy
                    violations
                  enum:
                  - UNSET_ENFORCEMENT
                  - SCALE_TO_ZERO_ENFORCEMENT
                  - UNSATISFIABLE_NODE_CONSTRAINT_ENFORCEMENT
                  - KILL_POD_ENFORCEMENT
                  - FAIL_KUBE_REQUEST_ENFORCEMENT
                  - FAIL_DEPLOYMENT_CREATE_ENFORCEMENT
                  - FAIL_DEPLOYMENT_UPDATE_ENFORCEMENT
                  type: string
                type: array
              eventSource:
                description: EventSource describes which events should trigger execution
                  of this policy
                enum:
                - NOT_APPLICABLE
                - DEPLOYMENT_EVENT
                - AUDIT_LOG_EVENT
                - NODE_EVENT
                type: string
              exclusions:
                description: |-
                  Exclusions define deployments or images that should be excluded from this policy
                  Supports workload label selectors for flexible exclusions
                items:
                  description: Exclusion defines what should be excluded from policy
                  properties:
                    deployment:
                      description: Deployment specifies deployment-based exclusions
                      properties:
                        name:
                          description: Name is the deployment name to exclude
                          type: string
                        scope:
                          description: Scope narrows the exclusion to specific namespaces
                          properties:
                            namespace:
                              description: Namespace is a direct namespace name to
                                target
                              type: string
                            namespaceSelector:
                              description: |-
                                NamespaceSelector is a Kubernetes label selector for namespaces
                                Targets all namespaces matching the selector
                                Example: matchLabels: {environment: production}
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            workloadSelector:
                              description: |-
                                WorkloadSelector is a Kubernetes label selector for workloads
                                Targets workloads (Deployments, Pods, etc.) matching the selector
                                Example: matchLabels: {app: frontend, tier: web}
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      type: object
                    expiration:
                      description: Expiration is when this exclusion expires
                      format: date-time
                      type: string
                    image:
                      description: Image specifies image-based exclusions
                      properties:
                        name:
                          description: Name is the image name to exclude (supports
                            wildcards)
                          type: string
                      type: object
                    name:
                      description: Name is a descriptive name for this exclusion
                      type: string
                    workloadSelector:
                      description: WorkloadSelector excludes workloads matching this
                        label selector
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              lifecycleStages:
                description: |-
                  LifecycleStages describes which policy lifecycle stages this policy applies to
                  For secured cluster policies: Only DEPLOY and RUNTIME are allowed (no BUILD)
                items:
                  description: |-
                    LifecycleStage defines the stage in the lifecycle where a policy applies
                    Note: For secured cluster policies (StackroxPolicy/ClusterStackroxPolicy),
                    only DEPLOY and RUNTIME are allowed. BUILD is only for Central SecurityPolicy.
                  enum:
                  - DEPLOY
                  - RUNTIME
                  type: string
                minItems: 1
                type: array
              mitreAttackVectors:
                description: MitreAttackVectors maps this policy to the MITRE ATT&CK
                  framework
                items:
                  description: MitreAttackVectors defines MITRE ATT&CK framework mapping
                  properties:
                    tactic:
                      description: Tactic is the MITRE ATT&CK tactic
                      type: string
                    techniques:
                      description: Techniques are the MITRE ATT&CK techniques
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              notifiers:
                description: |-
                  Notifiers is a list of names of notifiers that should be triggered
                  when a violation from this policy is identified
                  Note: Notifiers are resolved by sensor when sending alerts to Central
                items:
                  type: string
                type: array
              policyName:
                description: |-
                  PolicyName is the name of the policy as it appears in alerts
                  This field must be unique cluster-wide
                pattern: ^[^\n\r\$]{5,128}$
                type: string
              policySections:
                description: PolicySections define the violation criteria for this
                  policy
                items:
                  description: PolicySection defines a section of policy criteria
                  properties:
                    policyGroups:
                      description: |-
                        PolicyGroups is the set of policy groups that make up this section
                        Each group can be considered an individual criterion
                      items:
                        description: PolicyGroup defines a group of policy criteria
                        properties:
                          booleanOperator:
                            description: |-
                              BooleanOperator determines if the values are combined with OR or AND
                              Defaults to OR
                            enum:
                            - OR
                            - AND
                            type: string
                          fieldName:
                            description: |-
                              FieldName defines which field on a deployment or image this PolicyGroup evaluates
                              See https://docs.openshift.com/acs/operating/manage-security-policies.html#policy-criteria_manage-security-policies
                            type: string
                          negate:
                            description: |-
                              Negate determines if the evaluation of this PolicyGroup is negated
                              Defaults to false
                            type: boolean
                          values:
                            description: Values is the list of values for the specified
                              field
                            items:
                              description: PolicyValue defines a single policy value
                              properties:
                                value:
                                  description: Value is the string value
                                  type: string
                              type: object
                            type: array
                        required:
                        - fieldName
                        type: object
                      type: array
                    sectionName:
                      description: SectionName is a user-friendly name for this section
                      type: string
                  required:
                  - policyGroups
                  type: object
                minItems: 1
                type: array
              rationale:
                description: Rationale explains why this policy exists
                type: string
              remediation:
                description: Remediation describes how to remediate a violation of
                  this policy
                type: string
              scope:
                description: |-
                  Scope defines which workloads this policy targets across the cluster
                  Cluster-scoped policies can target resources in any namespace
                  All scope fields (namespace, namespaceSelector, workloadSelector) are allowed
                items:
                  description: |-
                    Scope defines scope for cluster-scoped policies
                    Cluster-scoped policies can target resources across all namespaces
                  properties:
                    namespace:
                      description: Namespace is a direct namespace name to target
                      type: string
                    namespaceSelector:
                      description: |-
                        NamespaceSelector is a Kubernetes label selector for namespaces
                        Targets all namespaces matching the selector
                        Example: matchLabels: {environment: production}
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    workloadSelector:
                      description: |-
                        WorkloadSelector is a Kubernetes label selector for workloads
                        Targets workloads (Deployments, Pods, etc.) matching the selector
                        Example: matchLabels: {app: frontend, tier: web}
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              severity:
                description: Severity defines how severe a violation from this policy
                  is
                enum:
                - UNSET_SEVERITY
                - LOW_SEVERITY
                - MEDIUM_SEVERITY
                - HIGH_SEVERITY
                - CRITICAL_SEVERITY
                type: string
            required:
            - categories
            - lifecycleStages
            - policyName
            - policySections
            - severity
            type: object
          status:
            description: ClusterStackroxPolicyStatus defines the observed state of
              ClusterStackroxPolicy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the policy's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastEvaluated:
                description: LastEvaluated is the timestamp of the last policy evaluation
                format: date-time
                type: string
              localPolicyId:
                description: |-
                  LocalPolicyID is the ID generated locally for this policy
                  This ID is used by sensor for policy evaluation and alert generation
                type: string
              violationMetrics:
                description: |-
                  ViolationMetrics tracks violations detected by this policy
                  Updated periodically as violations occur
                properties:
                  deploymentViolations:
                    description: |-
                      DeploymentViolations tracks per-deployment violation details
                      Limited to the most recent 100 deployments
                    items:
                      description: DeploymentViolation tracks violation metrics for
                        a specific deployment
                      properties:
                        deploymentRef:
                          description: DeploymentRef identifies the violating deployment
                          properties:
                            name:
                              description: Name is the deployment name
                              type: string
                            namespace:
                              description: |-
                                Namespace is the deployment namespace
                                Only populated for cluster-scoped policies
                                For namespace-scoped policies, the namespace is implicit
                              type: string
                          required:
                          - name
                          type: object
                        lastTriggered:
                          description: LastTriggered is the timestamp of the most
                            recent violation
                          format: date-time
                          type: string
                        violationCount:
                          description: ViolationCount is the cumulative count of violations
                            from this deployment
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - deploymentRef
                      - lastTriggered
                      - violationCount
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  lastViolationTime:
                    description: LastViolationTime is the timestamp of the most recent
                      violation
                    format: date-time
                    type: string
                  totalViolations:
                    description: TotalViolations is the cumulative count of all violations
                      detected
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
